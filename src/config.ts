import { ToolConfig, TransmuteConfig } from "./types";

export const defineConfig = (config: TransmuteConfig) => {
  return config;
};

export const defineTool = (config: ToolConfig) => {
  return config;
};
